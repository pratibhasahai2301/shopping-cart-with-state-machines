export function cartService(_, receive) {
    receive((event) => {
        switch (event.type) {
            case 'shipping_selected':

        }
    });

    const onShippingSelected = () => {

    }
}
